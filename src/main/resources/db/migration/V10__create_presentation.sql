create table presentation (id int8 not null,
                           description varchar(255),
                           file varchar(255),
                           name varchar(255),
                           primary key (id));
